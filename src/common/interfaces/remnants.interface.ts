
export interface IRemnantsItems {
    averageCost: number,
    remaining: number,
    quotaResource: number,
    quotas: number,
    residual: number,
    communityFund: number
}

export interface IRemnantsFilter {
    announcement: number,
    fund: number,
    trust: number  
}